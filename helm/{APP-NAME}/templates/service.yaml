apiVersion: v1
kind: Service
metadata:
  name: {{ include "fullname" . }}
  labels: {{- include "labels.common" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mcp
      name: mcp
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      name: metrics
  selector: {{- include "labels.selector" . | nindent 4 }}
